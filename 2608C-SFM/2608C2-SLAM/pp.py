
#----------------------------------------------------------------------

mapCapacity = cpu('mapCapacity')
print(mapCapacity)

print(type('mapCapacity'))
print(type('d_map'))

mapCapacity = cpu('mapCapacity')
d_map = gpu('d_map', 'f', mapCapacity)


plt.figure()
plt.plot(d_map)
plt.show()

#----------------------------------------------------------------------


n= cpu('n')
d_map = cpu('h_local', 'f', [n, 2])

x = d_map[:, 0]
y = d_map[:, 1]

plt.figure()
plt.plot(x, y, '.')
plt.show()

#----------------------------------------------------------------------


n= cpu('n')
d_local = gpu('d_local', 'f', [n, 2])

x = d_local[:, 0]
y = d_local[:, 1]

plt.figure()
plt.plot(x, y, '.')
plt.show()

#----------------------------------------------------------------------

n= 90
d_local = gpu('d_local', 'f', [n, 2])
d_transformed = mem('d_transformed', 'f', [n, 2])
d_map = mem('d_map', 'f', [n, 2])

x1 = d_local[:, 0]
y1 = d_local[:, 1]

x2 = d_transformed[:, 0]
y2 = d_transformed[:, 1]

x3 = d_map[:, 0]
y3 = d_map[:, 1]


fig, axes = plt.subplots(2, 2, figsize=(10, 10))
axes[0][0].plot(x1, y1, 'b.')
axes[0][1].plot(x2, y2, 'r.')
axes[1][0].plot(x3, y3, 'g.')
plt.show()



"""plt.figure()
plt.plot(x1, y1, 'b.')
plt.plot(x2, y2, 'r.')
plt.plot(x3, y3, 'g.')
plt.show()"""


print(cpu('n'))
print(cpu('blocks'))
print(cpu('THREADS'))

print(size('src'))

#----------------------------------------------------------------------
#----------------------------------------------------------------------

h_local = gpu('query', 'f', [90, 2])
x1 = h_local[:, 0]
y1 = h_local[:, 1]

plt.figure();
plt.plot(x1, y1, 'b')
plt.show()



h_local = gpu('query', 'f', [90, 2])
x1 = h_local[:, 0]
y1 = h_local[:, 1]

fig, axes = plt.subplots(2, 2, figsize=(10, 10))
axes[0][0].plot(x1, y1, 'b.')
axes[0][1].plot(x1, y1, 'r.')
axes[1][0].plot(x1, y1, 'g.')
plt.show()


#----------------------------------------------------------------------
#----------------------------------------------------------------------


h_idx = cpu('h_idx', 'i', [90])
plt.figure();
plt.plot(h_idx, 'r')
plt.show()

#----------------------------------------------------------------------


#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

#----------------------------------------------------------------------

