{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA-GDB",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/build/test",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CUDA Make",
            "internalConsoleOptions": "openOnSessionStart"
        }
    ]
}
