
sudo apt install nvidia-cuda-toolkit

nvcc --version

install extension cmake

sudo apt-get install -y freeglut3-dev libglew-dev libglfw3-dev

extension nsight
code --install-extension nvidia.nsight-vscode-edition 2>&1

sudo hostnamectl set-hostname mingjie

hostnamectl
hostname

cd /home/roots
mkdir share

ip a

ping mingjie
192.168.1.206

df -h

systemctl status ssh

sudo apt update
sudo apt install openssh-server

scp file.txt user@192.168.1.50:/home/user/
scp -r mydir/ user@host:/path/        # directory
scp user@host:/remote/file.txt ./     # pull instead of push

scp -r ./ roots@mingjie:/home/roots/share

rsync -avzn --progress ./ roots@mingjie:/home/roots/share



joey:

scp -r ./ roots@mingjie:/home/roots/share

scp -r roots@mingjie:/home/roots/share /home/roots/share


mingjie:

scp -r ./ roots@joey:/home/roots/share

scp -r roots@joey:/home/roots/share /home/roots/share


sudo apt install python3

sudo apt install python3-pip

python3 -m pip install numpy --break-system-packages


sudo apt remove --purge 'cuda*'
sudo apt autoremove -y

sudo rm -rf /usr/local/cuda*

sudo rm -f /usr/bin/nvcc
hash -r

sudo dpkg -i cuda-keyring_1.1-1_all.deb
sudo apt update
sudo apt install cuda-toolkit-13-0

vi ~/.bashrc

export CUDA_HOME=/usr/local/cuda
export PATH=${CUDA_HOME}/bin:${PATH}
export LD_LIBRARY_PATH=${CUDA_HOME}/lib64:$LD_LIBRARY_PATH

